{% extends "GajdawAngazeBundle::layout.html.twig" %}

{% block contents %}
    <h1>Room list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Tmp</th>
                <th>Actions</th>
                <th>Employee</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('room_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.tmp }}</td>
                <td><a href="{{ path('employee_show', { 'id': entity.employee.id }) }}">{{ entity.employee}}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('room_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('room_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('room_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
